CREATE TABLE Drawing_Revision (
company_id bigint,
project_id bigint,
drawing_date date,
drawing_id bigint,
drawing_sketches_count int,
height bigint,
id bigint,
number varchar(255),
pdf_size bigint,
pdf_url varchar(1000),
png_size bigint,
png_url varchar(1000),
received_date date,
title varchar(255),
updated_at datetime,
width bigint,
has_public_markup_layer_elements bit,
has_drawing_sketches bit,
current bit,
floorplan bit,
revision_number varchar(255),
thumbnail_url varchar(1000),
large_thumbnail_url varchar(1000),
activity_stream_last_viewed_at varchar(255),
position int,
zip_url varchar(1000),
obsolete bit,
drawing_set_id bigint,
drawing_area_id bigint,
discipline_id bigint,
discipline_name varchar(255),
discipline_position int,
PRIMARY KEY (company_id, project_id, )
);
